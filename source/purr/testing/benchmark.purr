language purr.core

module purr.testing.benchmark
  provides { _ time: _ repeat: _, _ suite: _ }
  uses purr.intrinsics.primitives { bench_time, bench_suite }

  define desc time: fn repeat: n = bench_time(desc, fn, n)
  define desc suite: benchs = bench_suite(desc, benchs)